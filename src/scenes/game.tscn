[gd_scene load_steps=13 format=3 uid="uid://drhm05kil6jj0"]

[ext_resource type="Script" path="res://assets/game_objects/camera/camera.gd" id="1_ek21h"]
[ext_resource type="Script" path="res://scripts/mouse.gd" id="1_ikwiy"]
[ext_resource type="PackedScene" uid="uid://by15vcsccwjr" path="res://assets/graphics/background.tscn" id="2_6upci"]
[ext_resource type="PackedScene" uid="uid://dbxffv6mxkqtd" path="res://assets/game_objects/empty_town/empty_town.tscn" id="2_xqc5c"]

[sub_resource type="Shader" id="Shader_ce4lb"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform vec4 line_color : source_color = vec4(1);
uniform float line_thickness : hint_range(0, 10) = 1.0;

void fragment() {
	vec2 size = TEXTURE_PIXEL_SIZE * line_thickness;
	
	float outline = texture(TEXTURE, UV + vec2(-size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
	outline = min(outline, 1.0);
	
	vec4 color = texture(TEXTURE, UV);
	COLOR = mix(color, line_color, outline - color.a);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_iqkqp"]
resource_local_to_scene = true
shader = SubResource("Shader_ce4lb")
shader_parameter/line_color = Color(1, 1, 1, 0)
shader_parameter/line_thickness = 0.0

[sub_resource type="Shader" id="Shader_mtwqh"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform vec4 line_color : source_color = vec4(1);
uniform float line_thickness : hint_range(0, 10) = 1.0;

void fragment() {
	vec2 size = TEXTURE_PIXEL_SIZE * line_thickness;
	
	float outline = texture(TEXTURE, UV + vec2(-size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
	outline = min(outline, 1.0);
	
	vec4 color = texture(TEXTURE, UV);
	COLOR = mix(color, line_color, outline - color.a);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_tjtgf"]
resource_local_to_scene = true
shader = SubResource("Shader_mtwqh")
shader_parameter/line_color = Color(1, 1, 1, 0)
shader_parameter/line_thickness = 0.0

[sub_resource type="Shader" id="Shader_62rfh"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform vec4 line_color : source_color = vec4(1);
uniform float line_thickness : hint_range(0, 10) = 1.0;

void fragment() {
	vec2 size = TEXTURE_PIXEL_SIZE * line_thickness;
	
	float outline = texture(TEXTURE, UV + vec2(-size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
	outline = min(outline, 1.0);
	
	vec4 color = texture(TEXTURE, UV);
	COLOR = mix(color, line_color, outline - color.a);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_dylh1"]
resource_local_to_scene = true
shader = SubResource("Shader_62rfh")
shader_parameter/line_color = Color(1, 1, 1, 0)
shader_parameter/line_thickness = 0.0

[sub_resource type="Shader" id="Shader_wtxjx"]
resource_local_to_scene = true
code = "shader_type canvas_item;

uniform vec4 line_color : source_color = vec4(1);
uniform float line_thickness : hint_range(0, 10) = 1.0;

void fragment() {
	vec2 size = TEXTURE_PIXEL_SIZE * line_thickness;
	
	float outline = texture(TEXTURE, UV + vec2(-size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, 0)).a;
	outline += texture(TEXTURE, UV + vec2(0, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, size.y)).a;
	outline += texture(TEXTURE, UV + vec2(-size.x, -size.y)).a;
	outline += texture(TEXTURE, UV + vec2(size.x, -size.y)).a;
	outline = min(outline, 1.0);
	
	vec4 color = texture(TEXTURE, UV);
	COLOR = mix(color, line_color, outline - color.a);
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_ojety"]
resource_local_to_scene = true
shader = SubResource("Shader_wtxjx")
shader_parameter/line_color = Color(1, 1, 1, 0)
shader_parameter/line_thickness = 0.0

[node name="Game" type="Node"]

[node name="Mouse" type="Node" parent="."]
script = ExtResource("1_ikwiy")

[node name="UI_Group" type="Node" parent="."]

[node name="CanvasModulate" type="CanvasModulate" parent="."]

[node name="Background" parent="CanvasModulate" instance=ExtResource("2_6upci")]
unique_name_in_owner = true

[node name="Peoples" type="Node" parent="CanvasModulate"]

[node name="Town_Buildings" type="Node" parent="CanvasModulate"]

[node name="empty_town" parent="CanvasModulate/Town_Buildings" instance=ExtResource("2_xqc5c")]
material = SubResource("ShaderMaterial_iqkqp")
position = Vector2(1483, 483)

[node name="empty_town2" parent="CanvasModulate/Town_Buildings" instance=ExtResource("2_xqc5c")]
material = SubResource("ShaderMaterial_tjtgf")
position = Vector2(1685, 283)

[node name="empty_town3" parent="CanvasModulate/Town_Buildings" instance=ExtResource("2_xqc5c")]
material = SubResource("ShaderMaterial_dylh1")
position = Vector2(1504, 686)

[node name="empty_town4" parent="CanvasModulate/Town_Buildings" instance=ExtResource("2_xqc5c")]
material = SubResource("ShaderMaterial_ojety")
position = Vector2(1708, 879)

[node name="Land_Buildings" type="Node" parent="CanvasModulate"]

[node name="Camera" type="Camera2D" parent="."]
anchor_mode = 0
current = true
limit_left = 0
limit_top = 0
limit_right = 4040
limit_bottom = 1080
limit_smoothed = true
position_smoothing_enabled = true
drag_horizontal_enabled = true
script = ExtResource("1_ek21h")
speed = 2000.0
mouse_center_area = 1600.0
